# دليل تسجيل الدخول المحلي

## 🔐 بيانات تسجيل الدخول المحلية

تم إضافة نظام تسجيل دخول محلي للتطوير والاختبار. يمكنك استخدام البيانات التالية:

### المستخدمين المتاحين

| اسم المستخدم | كلمة المرور | الوصف |
|--------------|-------------|-------|
| `admin` | `96579657` | مدير النظام |
| `test` | `123456` | مستخدم تجريبي |
| `demo` | `demo123` | مستخدم تجريبي |

## 🚀 كيفية الاستخدام

1. **اذهب إلى صفحة تسجيل الدخول** (`/auth/login`)
2. **أدخل اسم المستخدم:** `admin`
3. **أدخل كلمة المرور:** `96579657`
4. **اضغط على "تسجيل الدخول"**

## ✨ المميزات

- ✅ **تسجيل دخول فوري** بدون الحاجة للخادم
- ✅ **صلاحيات إدارية كاملة** للمستخدم المحلي
- ✅ **توافق مع النظام الأصلي** - يمكن استخدام البيانات المحلية أو الخادم
- ✅ **رسائل واضحة** تظهر بيانات تسجيل الدخول على الصفحة

## 🔧 كيفية إضافة مستخدمين جدد

لتعديل أو إضافة مستخدمين جدد، افتح ملف:
`src/app/pages/auth/login/login.component.ts`

وابحث عن دالة `checkLocalCredentials` وعدل المصفوفة:

```typescript
const localUsers = [
    { username: 'admin', password: '96579657' },
    { username: 'test', password: '123456' },
    { username: 'demo', password: 'demo123' },
    // أضف مستخدمين جدد هنا
    { username: 'newuser', password: 'newpass' }
];
```

## 🎯 ملاحظات مهمة

- البيانات المحلية **للتطوير والاختبار فقط**
- النظام يدعم **كلا النوعين** من تسجيل الدخول (محلي وخادم)
- المستخدمين المحليين يحصلون على **صلاحيات إدارية كاملة**
- الرموز الوهمية صالحة لمدة **24 ساعة**

---
**تم التطوير بواسطة:** AI Assistant
**التاريخ:** $(Get-Date -Format "yyyy-MM-dd")
