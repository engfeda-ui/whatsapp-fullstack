# Frontend exposure (host port -> container port 80)
FRONTEND_PORT=8080

# Backend exposure (host port -> container port 8080)
BACKEND_PORT=8080

# Backend configuration overrides
BACKEND_CONNECTION_STRING=Data Source=/data/whatsapp.db
JWT_SECRET=change_this_jwt_secret
JWT_ISSUER=WhatsAppBackend
JWT_AUDIENCE=WhatsAppFrontend
FRONTEND_ORIGIN=http://localhost:8080
